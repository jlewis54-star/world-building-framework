# Examples

Optional worked sample worlds may live here later.

v1 ships process and templates only. No sample IP yet. A thin public-domain or original sample can live under `examples/<slug>/` mirroring `world/`. Mark it non-authoritative relative to each cloner's `world/`.
