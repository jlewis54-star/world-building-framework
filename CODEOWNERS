# Default owners for review routing (GitHub CODEOWNERS).
# After clone, replace @YOUR_GITHUB_HANDLE with real team handles.
# Leave paths commented until you set owners — a fresh clone must not
# assign reviews to a stranger's handle.

# /world/ @YOUR_GITHUB_HANDLE
# /ai/agents/ @YOUR_GITHUB_HANDLE
# /playbook/ @YOUR_GITHUB_HANDLE
