#!/usr/bin/env bash
# Opt-in pre-commit: git config core.hooksPath .githooks
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"

if [[ -x "$ROOT/scripts/validate.sh" ]]; then
  "$ROOT/scripts/validate.sh" --changed
fi

exit 0
